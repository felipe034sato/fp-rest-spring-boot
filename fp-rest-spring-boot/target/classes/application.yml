spring:
  application:
    name: fp-rest-spring-boot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fp_rest_spring_boot?useTimezone=true&serverTimezone=UTC
    username: root
    password: Lipe@123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
    open-in-view: false
logging:
  level:
    root: WARN
    #br.com.felipe034sato: DEBUG