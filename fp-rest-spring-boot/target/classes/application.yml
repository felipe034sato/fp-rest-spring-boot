spring:
  application:
    name: fp-rest-spring-boot

  datasource:
    url: jdbc:mysql://localhost:3306/fp_rest_spring_boot
    username: root
    password: Lipe@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
    open-in-view: false

logging:
  level:
    root: WARN

kind: Application